# Sanity connection for the public (read-only) site.
# Copy to .env.local and fill in your project id. Until this is set, the site
# renders from the static fallback content in src/data/*.
VITE_SANITY_PROJECT_ID=your-project-id
VITE_SANITY_DATASET=production
VITE_SANITY_API_VERSION=2024-10-01
